-- use bakery;

-- select database();

-- exercise

-- create	table inventory(
-- 	item_name varchar(100),
--     price decimal(7,2),
--     quantity int(100)
-- );

-- select curtime() as Time;

-- select current_date() as Date;

-- select dayofweek(current_date()) as 'Date Of The Week';

-- select dayname(current_date()) as "Day name";

-- select date_format(current_date(),'%m/%d/%Y') as DATE

-- select date_format(now(),'%M %D at %k:%i') as 'Month format'

-- create table Tweets_table (
-- 	tweet varchar(150),
--     username varchar(100),
--     created_at timestamp default current_timestamp
-- );
